@model IEnumerable<Concurso.App.gestion.Application.DTOs.UsuarioDto>
@{
    ViewData["Title"] = "Usuarios";
}
<div class="card shadow-sm mb-4">
    <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
        <span><i class="fa-solid fa-user"></i> Usuarios</span>
        <a class="btn btn-success btn-sm gradient-blue" asp-action="Create"><i class="fa fa-plus"></i> Nuevo Usuario</a>
    </div>
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-hover table-striped align-middle">
                <thead class="table-light">
                    <tr>
                        <th>Id</th>
                        <th>Nombre</th>
                        <th>Correo</th>
                        <th class="text-end">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Id</td>
                        <td>@item.Nombre</td>
                        <td>@item.Correo</td>
                        <td class="text-end">
                            <a class="btn btn-sm btn-info me-1" asp-action="Edit" asp-route-id="@item.Id"><i class="fa fa-edit"></i></a>
                            <a class="btn btn-sm btn-danger" asp-action="Delete" asp-route-id="@item.Id"><i class="fa fa-trash"></i></a>
                        </td>
                    </tr>
                }
                </tbody>
            </table>
        </div>
    </div>
</div>
